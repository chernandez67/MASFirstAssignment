import {View, Text, Button} from 'react-native';
import React from 'react';

const List = ({ navigation }) => {
    return (
        <View>
            <Text>Details</Text>
        </View>
    )
};

export default List